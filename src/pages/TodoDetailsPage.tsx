import React from 'react';
import { Container, Typography } from '@mui/material';

const TodoDetailsPage: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Detalhes da Atividade</Typography>
      {}
    </Container>
  );
};

export default TodoDetailsPage;
